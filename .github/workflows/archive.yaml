name: Archive meetup data

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron: "0 */12 * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  meetup:
    name: Archive meetup data
    runs-on: ubuntu-latest
    env:
      "meetupr:token": ${{ secrets.meetupr_token }}
      "meetupr:token_file": ${{ secrets.meetupr_token_file}}
      RENV_PATHS_ROOT: ~/.cache/R/renv
      RENV_PROFILE: archive
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install cURL Headers
        run: |
          sudo apt-get update
          sudo apt-get install libcurl4-openssl-dev libsecret-1-dev

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "renv"
          use-public-rspm: true

      - name: Cache packages
        uses: actions/cache@v4
        with:
          path: ${{ env.RENV_PATHS_ROOT }}
          key: ${{ runner.os }}-renv-${{ hashFiles('**/renv.lock') }}
          restore-keys: |
            ${{ runner.os }}-renv-

      - name: Restore packages
        shell: Rscript {0}
        run: |
          if (!requireNamespace("renv", quietly = TRUE)) install.packages("renv")
          renv::install("promises")
          renv::install("httpuv")
          renv::restore()

      - name: Meetup authentication
        run: Rscript -e "meetupr::meetup_ci_load()"

      - name: Update chapter data
        run: Rscript scripts/get_chapters.R

      - name: Update event data
        run: Rscript scripts/get_events.R

      - name: Commit data
        if: github.event_name == 'schedule' || github.event_name == 'push'
        run: |
          git config --local user.email "actions@github.com"	
          git config --local user.name "GitHub Actions"	
          git add data/	
          git commit -m 'Update meetup data' || echo "No meetup data to commit"	
          git push || echo "Nothing to push"
